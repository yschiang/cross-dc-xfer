# System Design 決策紀錄（grilling 2026-09-21 起）

供撰寫 docs/design/system-design.md 用。詞彙見 CONTEXT.md，需求見 spec v0.3。

| # | 決策 | 封閉的需求 |
| --- | --- | --- |
| D1 | Source Ready 權威紀錄 = NFS 上與檔案同目錄的 `.manifest`（identity、Data class、size、digest、Finalize 時戳）；本地 DB 只是可由 NFS 全掃重建的索引與工作狀態 | FR-02, RR-02, T18, T29 |
| D2 | NFS 路徑由 Framework 從 identity 推導：`<namespace>/<data class>/<yyyy-mm-dd>/<HH>/<logical key>`，日期時間為 Finalize 時 Source 時鐘；Application 不自選路徑 | FR-04, RC-01, §17 inspection |
| D3 | Finalize 協議（v2，修正並發與對稱）：① 內容寫 `<key>.<uuid>.writing` + fsync；② `<key>.manifest` 以 O_EXCL create + write + fsync（EEXIST → 讀既有 manifest 比 digest：同 → 冪等成功、不同 → CONFLICT）；③ `link(<key>.<uuid>.writing, <key>)`——目標已存在則原子失敗，**成功 = commit point = Source Ready**；④ unlink `.writing`。兩端一致：**`<key>` 存在 = Ready**（Target 亦以 link 發布）。Framework 額外 NFS 成本 = 每筆 1 create + 1 fsync + 1 link + 1 unlink。Manifest 單行 JSON 含 schema_version、uuid。Rediscovery：manifest 有 + `<key>` 有 = Ready；manifest 有 + `<key>` 無 + `.writing` 有 → 補 link；manifest 有 + 兩者皆無 → Application 從未拿到 SUCCESS，標 manifest Abandoned；只有 `.writing` = Writing / Abandoned 候選 | FR-02, FR-03, FR-04, SR-03, SR-04, SR-06, T20, T29 |
| D3a | Finalize 冪等重試規則（PENDING_CONFIRMATION 後）：manifest EEXIST 且內容相同 → 續行；不同 → FR-04 conflict；link EEXIST 且 `<key>` digest 等於 manifest → 已完成；manifest 半截（parse 失敗）→ 以 O_EXCL 失敗處理，改走 unlink + 重建。NFS 不可靠性被壓縮為「Finalize 可能需呼叫兩次」，不會留下狀態不明 | SR-04, FR-04, T29 |
| D3b | NFS mount 要求：Application 主機與 sync 主機皆用 `hard`（不假失敗，hang 交給 SR-05 隔離）；library 以獨立 thread + timeout 包每個 NFS 操作；sync 主機另加 `lookupcache=positive`、調低 `acdirmax` 以縮短新檔可見延遲。以上列入 SR-03 驗證項 | SR-02, SR-03, SR-05 |
| D4 | digest 由 Application 端 library 於 write 時 streaming 計算，Finalize 直接寫入 manifest；不重讀、不重算 | IR-01, DG-02 |
| D5 | 每 Node 單一 sync 主機、單一 process，靠監控快速重啟；不做 active-standby；sync service 停機只影響 lag 不影響 Local Transaction | DG-02, DG-05, T06 |
| D5 註 | 應有集合 = NFS manifest × Policy（NFS 為真相）；義務進度 = sync service 本地 DB（可由 manifest 全掃 + 向 Target 查 Completion evidence 重建）；Application 不碰 DB | RR-02, RR-03 |
| D7 | Application 現有 DB 為每 Phase 自有（Node-local）；sync service 共用同一顆 DB、獨立 schema，sync tables 僅由 sync service 寫入；Application 的「檔案已 Ready」記帳不是 Framework 真相 | §1.2, DG-01, RR-02 |
| D8 | Application 整合規則：write → Finalize → 拿到 SUCCESS 才 commit 業務交易並記錄 identity；PENDING_CONFIRMATION 時不 commit，重試 Finalize 至確定；重跑交易前先 exists(identity)。Finalize 後 rollback 產生的孤兒 Ready 檔為可接受浪費。Framework 不參與 Application 的 DB 交易 | SR-01, SR-04, T29 |
| D9 | Node-local DB 為 Oracle；本機開發跑不起 Oracle → SQL 保持可攜（JPA / 標準 SQL、不用 Oracle 專屬語法），本機與單元測試用 H2 Oracle mode，CI 用 Oracle Free container 跑整合測試。單一 sync process 為唯一寫入者，不需要 row-level 搶鎖（SKIP LOCKED） | D5, D7 |
| D10 | 掃描三層共用同一段冪等 ingest：增量掃（當前 + 前一小時，每 N 秒）、全量對帳掃（最近 D 天，預設 7，每 M 小時，附截止點與覆蓋範圍）、重建掃（人工觸發、全部）；超出 D 天的範圍在 metric 揭露為未對帳 | RC-01, RC-02, RC-04, T11 |
| D11 | Writing / Abandoned 由掃描順手判定：`.writing` 或無 manifest 的內容檔 mtime 超過 TTL（預設 24h，operational policy）→ DB 記 Abandoned，檔案留原地不搬，掃描忽略；實際刪除依 RT-01 Framework 清理政策 | FR-02, FR-03, RT-01 |
| D12 | 傳輸 = Target pull over HTTP：Target sync service 週期 `GET /pending?target=<self>&limit=N`（Source 回該 Target 最舊 N 筆「未完成且 next_attempt_at 已到」的義務，附 manifest 欄位；**無 cursor**）→ `GET /file/<identity>` 兩端 streaming（Source 從自身 NAS 讀、Target 寫自身 NAS 同路徑 `.writing` 並以 DigestInputStream 驗 digest；禁止整檔進記憶體）→ size/digest 符合則 rename 發布、DB 寫 received row → `POST /report {kind: DONE}`。Target 唯一狀態 = in-memory in-flight set；重複拿到同一筆先查 received 表，冪等。`/report` 遺失（T19）：下輪 pending 重拿 → Target 見正式路徑已有檔 → 重算該檔 digest 對基準 → 補 row、重送 DONE，不重下載。**不寫 `.evidence` 檔**；Target DB 遺失 → 掃正式檔重算 digest 重建。整檔重傳、size-based timeout；續傳留待 GB 級 | RR-01, RR-03, RR-04, FR-03, IR-02, T08, T19 |
| D12a | 失敗處理：Target 單筆失敗刪 `.writing`、`POST /report {kind: FAILED, reason}`；**退避狀態只在 Source**：收到 FAILED 設該義務 next_attempt_at（指數退避，上限 15 min）、累計次數，`/pending` 自動排除未到期者；反覆 digest 不符 / 4xx → QUARANTINED；Target 回報 CAPACITY → BLOCKED。Target 停機 = 不來拉，Source 以「該 Target 上次 pending 時間」staleness 推導 UNREACHABLE，不需 circuit breaker / 429 / 探測。Catch-up 節奏由 Target 依自身 NAS 負載決定（並發、bytes/s 為 Target 端 operational policy） | RR-04, RR-05, §14, §16, T12, T16, T17 |
| D12 否決 | Source push（需三層流控）、NFS-to-NFS 直搬（遠端 NAS hang 污染本機 NFS client，違反 SR-05）、rsync（仍需 Target 服務做驗證與 evidence）、NAS 原生複寫（volume 級、不可依 Data class 選 Target、發布時機不受控） | SR-05, DG-01 |
| 事實 | 目前實際單一 Phase 最長停機約 6 小時；spec 的 24 小時維持為容量與驗收 envelope（約 4× 餘裕），不下修 | §1.1, §18, RT-02 |
| D6 | 新檔發現 = 純掃描（D10 三層），不做 library → sync service 通知；正常 lag ≈ 掃描間隔 + 目錄 cache（D3b 調參後 ~10–15 s）。若 §18 lag SLO 要求秒級，再加 HTTP fire-and-forget 通知（純加法） | RC-01, DG-02 |
| D13 | `/pending` 排序 = source_ready_at FIFO；不做 Data class 優先級、不分快慢通道（留為純加法） | RR-05, §16 oldest_unfinished_age, §20 |
| D14 | Node 間 HTTP 認證 = Fab 內 shared token（header）+ HTTPS；token 為 operational policy 可輪替；不做 mTLS、不裸跑 | DG-04, §13, §17 |
| D15 | Target 端索引：已發布檔案寫自身 DB received 表（identity、size、digest、published_at）；Target 真相 = 正式檔 + received row，無 `.evidence` 檔。consumer 查詢走 DB：Policy 說不該來 → NOT_EXPECTED，該來且 received 無 → DATA_NOT_READY；不反問 Source | SR-06, T28, RR-03 |
| D16 | Deep check 由 Target 自己滾動讀本地 NAS 重算 digest，`POST /report {kind: VERIFIED}` 回報 Source；Source 比對不符 → 重開該義務（保留原完成紀錄 + 新發現），下輪 pending 重新給 Target 拉 | RC-02, IR-03, T30 |
| D17 | 沒有 CP process。Config（Policy + operational policy + version）為 git repo 中不可變的 `configs/v<N>.json`；發布 = merge 改 `latest` 的 PR（review = 授權，commit = 發布者/時間稽核）；下發走既有 CD pipeline，寫到各 sync 主機本機磁碟 `candidate.json.tmp` → rename。Node 端：驗證（schema、版本遞增、**Policy 段須與 active 完全相同**，否則拒絕並上報）→ rename active→lkg、candidate→active → 熱載入 operational policy。啟動只讀本機 active.json，缺則 lkg，皆缺拒絕啟動。各 Node 曝 `active_config_version` metric 供全域彙整。CD / git 不可用 = 不能發新版，資料流不受影響 | §11, §12, §13, AC-CP-01~03, AC-CFG-01~05, T15, T22, T23, T26 |
| D17 事實 | CI 與 CD 分開；CD 已能部署到 sync 主機。git server 種類與 metrics 收集工具未定；metrics 先以 Spring Boot Actuator 曝 Prometheus 格式 | §16 |
| D18 | Application library 的 Policy 來源 = 本 Node sync service `GET /policy`（sync service 記憶體中 active config 的 Policy 段）；library 記憶體快取 + App 主機本機 fallback 檔；sync service 不可達用快取，首次啟動且無快取 → 拒絕寫入並明確回報。CD 不送 config 到 App 主機。Config 四層：git（真相）→ sync 主機本機檔 → sync service 記憶體 → library 快取 | SR-01, DG-02, §11 |
| D19 | Shallow check 三步：① NFS manifest 集合 vs DB identity 集合，差集 ingest；② 對每個 Target `GET /received?since=<cursor>` 增量比對義務表——Source COMPLETED 但 Target 無 → 重開、Target 有但 Source 未完成 → 補 COMPLETED、digest 紀錄不符 → QUARANTINED；③ 寫 inspection 紀錄（範圍、截止點、比對筆數、unknown 筆數）。Target 不可達的部分標 unknown，保留上次結果與時間 | RC-01, RC-02, RC-04, T11, T19, T27, T30 |
| D20 | Ops 介面：每 Node sync service 提供本地 ops HTTP API（`GET /status` 回 §15 十題的本地答案、retry / pause / trigger-reconciliation / config 檢視），一支 CLI 包它；全域視圖 = CLI 依 Policy 的 Node 清單扇出 `GET /status` 印表，不可達者標 unknown + 上次成功時間；無 Web UI；metrics 以 Actuator 曝出為附加，不是全域視圖的前提。操作授權 = ops API token，操作者 / 時間 / 範圍 / 理由 / 結果寫 DB | §11, §15, §17, AC-CP-03 |
| D21 | 容量：library 自己對 NAS mount statfs（快取 10 s），write 前低於 reject 門檻即明確拒絕不寫；門檻自快取的 Policy 取得。sync service 亦 statfs 供 usable_free_bytes / capacity_alert_status metric 與告警。Target 端拉取前同一段檢查，低於門檻停止拉取並回報 CAPACITY → Source 標 BLOCKED。不透過 `/policy` 傳遞容量狀態 | RT-02, T21 |
| D22 | Pause toward a Target 只在 Source 端：ops 對 Source 下 pause(B) → 義務標 BLOCKED（reason: paused by ops）並自 `/pending` 排除；義務照常計入 backlog 與 age；resume 解除。CLI 扇出可對所有 Source 一次下達。Target 端不做 pause——B 整體維護直接停 B 的 sync service 即可 | §14, §17, T31 |
| 簡化審查 | 已否決的複雜度：`.evidence` 檔（每 Target 最多 9×10⁵ 次/日小檔寫入，重建改用重算 digest）、Abandoned 搬目錄、`/pending` cursor + Target 端退避、三個回報端點（合併為 `/report`）。保留：`/HH/` 小時目錄（否則單日目錄 10⁵ 項）、App 主機 Policy fallback 檔（保護 Local Transaction 不依賴本地 sync service） | — |
| D23 | Application library 介面：`beginWrite(namespace, dataClass, logicalKey)` → WriteHandle（驗 Policy、容量；OutputStream）；`finalize()` → SUCCESS / FAILURE(reason) / PENDING_CONFIRMATION；`discard()` 僅 finalize 前；`exists(identity)` → READY / DATA_NOT_READY / NOT_EXPECTED / UNAVAILABLE（一次 stat `<key>` + Policy 判定，不碰 DB / sync service）；`read(identity)` 僅 READY 時給 InputStream。不提供 list / delete / rename。每個 NFS 操作獨立 thread + timeout。Spring Boot starter 打包；設定僅 mount root、sync service URL、timeout | SR-01, SR-05, SR-06, FR-01 |
| D24 | DB schema（Oracle，sync service 自有 schema）：`file_identity`(source_node, namespace, logical_key, data_class, size, digest, source_ready_at, path)；`obligation`(identity_ref, target_node, state, attempts, next_attempt_at, last_error, completed_at)，索引 (target_node, state, next_attempt_at)；`received`(identity_ref, size, digest, published_at)；`inspection`(scope, cutoff, started_at, finished_at, checked, unknown, diffs, repairs)；`ops_audit`(who, at, action, scope, reason, result)；`target_liveness`(target_node, last_pending_at) | RR-02, RC-04, §14, §16, §17 |
| D25 | 解除 QUARANTINED：release = attempts 清零、next_attempt_at 歸零、狀態回 PENDING，重走完整驗證；Target 正式路徑已有不符檔者，ops 先以 ops API `evict --target <node> <identity>`（寫 audit）刪除再 release。不提供 force-complete | §17, IR-03, T17 |
| D26 | Node 內時鐘：App 主機與 sync 主機 NTP 同步為部署前提；lag 起點 = manifest `source_ready_at`（library 打），終點 = sync service 收到 DONE；增量掃描範圍 = 當前 + 前兩小時，吸收小時邊界偏差 | §16, §18 Measurement, D6, D10 |
| D27 | Metrics 管線：Node 端只曝帶標籤的原始值（Completeness / Freshness / Integrity 帶 `{source,target}`，Availability / Trust 帶 `{node}`），不做跨 Node 計算；sync service 與 library 以 Spring Boot Actuator 曝 Prometheus 格式，Prometheus 抓各 Node，Grafana 呈現 overall（age 取 max、backlog 取 sum）→ by phase → by target 下鑽。「停止更新」告警 = Prometheus `up{node}` 與 `absent()`；CLI 扇出 `GET /status` 保留給 ops 操作與 metrics 管線不可用時的備援。告警走 Alertmanager 或既有 NMS 待定。儀表板 JSON 與告警規則進 repo 為交付物。詳見 docs/design/monitoring.md | §11, §15, §16, D17 事實, D20 |
| D33 | 告警門檻（首版，皆為 operational policy 可調）：`oldest_unfinished_age` > 2 h 警告、> 12 h 事故；`unrecoverable_count` / `integrity_failure_count` / `identity_conflict_count` > 0 事故；`last_complete_scan_time` 過期 > 2 個掃描週期警告、`unknown_count` 持續 > 1 個週期警告；`capacity_alert_status` 進 alert 警告、進 reject 事故；`up{node}` 抓不到 > 3 個抓取週期警告；`local_write_availability` / latency 門檻綁 §18 TBD。每軸第一層只有一個告警指標，其餘為診斷 | §16, §18, DG-03, DG-04, RT-02 |
| D28 | Target 端下載檔名固定為 `<key>.writing`（不帶 uuid，開檔即 truncate）：Target 單 process + in-memory in-flight set 已排除同 identity 並發，crash 後下輪重拉直接覆蓋，不留孤兒。永不重拉者（義務在 Source 被隔離、或由對帳補 COMPLETED）由 Target Deep check 掃描順手清：`.writing` 不在 in-flight set 且 mtime 超過傳輸 timeout → 刪 | RR-03, RR-04, RT-01, T08 |
| D29 | sync DB 重建 = ops API `rebuild`（寫 audit）：① 清 `file_identity`、`obligation`、`received`、`target_liveness`，保留 `inspection` 與 `ops_audit`；② 重建掃，範圍與 D10 全量對帳掃相同（最近 D 天），義務全建為 PENDING；③ 對每個 Target `GET /received?since=0` 全量比對，digest 相符者標 COMPLETED；④ 寫 scope=rebuild 的 inspection。重建期間曝 `rebuild_in_progress{node}=1`，告警規則以此抑制 age / backlog 假警報；`/pending` 照常服務。超過 D 天的歷史一律標未對帳，避免重送 Target 已依 retention 清除的檔 | RR-02, RC-04, D5 註, D10, T18 |
| D30 | 首版參數預設（皆 operational policy）：增量掃描 10 s；Target 輪詢 `/pending` 5 s；`/pending` limit 200；HTTP 傳輸 timeout = 30 s + size ÷ 1 MB/s；Target 並發下載 4；Target 總拉取速率上限 50 MB/s（10 GbE 下占 4%，可調高）；UNREACHABLE = 連續 6 個輪詢間隔無 pending；全量對帳掃每 6 h、D = 7 天；退避上限 15 min（D12a）；Abandoned TTL 24 h（D11） | RR-04, RR-05, RC-02, §18 |
| D31 | Catch-up 算術：envelope 最壞 Target 停 24 h 欠 1 TB，恢復期間新流量同為 1 TB/日，24 h 內追平需淨排空約 23 MB/s；50 MB/s 上限餘裕約 2×。實際停機 6 h 最壞欠 250 GB，餘裕約 16×。檔案數 2×10⁵ 筆 / 24 h ≈ 2.3 筆/s，`/pending` 上限 40 筆/s，非瓶頸。Pull 模型結構性優勢：Source 停機時其 Application 同停、不產新檔，Source 恢復無 backlog；只有 Target 停機才有 catch-up，且 Target 以自身總速率上限自限，九個 Source 同時供給也不會塞爆。填 §18 Recovery TBD = 24 h | DG-03, RR-05, §18, T12 |
| D32 | DB 列保留：`file_identity` 與 `obligation` 列在對應檔案依 RT-01 自 NAS 清除後再保留 30 天；Target `received` 列同規則；由全量對帳掃順手 purge。`inspection` 保留 1 年；`ops_audit` 不清。以 NAS 保留 30 天估算：obligation 每日 9×10⁵ 列，穩態約 5.4×10⁷ 列（60 天），Oracle 無壓力；D29 重建最大範圍亦受此限 | RT-01, RR-02, §16 |
| 事實 | sync 主機網卡 10 GbE；NAS 上 Ready 檔保留 30 天；實際單一 Target 最長停機約 6 h（與前列 Phase 停機事實一致，24 h 仍為 envelope） | §18, RT-01, D30, D31, D32 |
| D34 | Fast recovery：process 無狀態（狀態全在 NFS 與 DB），重啟不重建。重啟由 systemd `Restart=always`、`RestartSec=5` 負責，health check 打 Actuator `/health`，不用 k8s。啟動序列 = 讀 active.json → 開 DB → 起 HTTP → 起增量掃描；不做全量掃，目標 30 s 內開始服務 `/pending`，全量對帳掃照原排程。Target 重啟只丟 in-memory in-flight set，下輪輪詢重拉；library 用快取 Policy，sync service 停機不影響 Local Transaction。downtime 以 Actuator `process_start_time` + Prometheus `up` 計算，不另加指標。DB 重建（D29）為災難路徑，不在此範圍 | DG-02, DG-05, D5, D12, D18, T06 |
| D35 | 清道夫：sync service 每日一次排程（operational policy），三項職責：① Source 端 `.writing` 與無 `<key>` 的 manifest 超過 Abandoned TTL → 刪；② Target 端 `.writing` 不在 in-flight set 且 mtime 超過傳輸 timeout → 刪；③ DB 列依時間規則 purge：`file_identity` / `obligation` / `received` 的 source_ready_at 早於 NAS 保留期（30 天）+ 30 天即刪，不觀察 NAS 何時刪檔；`inspection` 1 年、`ops_audit` 不清。紅線：永不刪 `<key>` 正式檔、永不刪有未完成義務的 manifest（RT-01）。曝 `cleanup_deleted_count{kind}`。D11、D28、D32 的刪除動作皆改由此執行 | RT-01, D11, D28, D32 |
| D36 | 增量掃描比對用 JVM 記憶體的已知 key 集合：`Map<hourDir, Set<key>>`，只有集合中沒有的 key 才查 DB 走冪等 ingest；時窗滑動整桶丟棄。啟動時一句查詢載入最近 3 小時的 key（約 1.3 萬筆）。真相仍在 DB，集合純為衍生快取 | D10, D26, D34 |
| D2 修 | 路徑第一段加 `<source node>`：`<source node>/<namespace>/<data class>/<yyyy-mm-dd>/<HH>/<key>`，Source 自己也寫自己的 node 名，兩端路徑完全一致。理由：Namespace 每 Application 一個，同一 Application 跑在多個 Phase 時同 key 會在 Target 撞路徑；identity 三元組完整映射到路徑後 rediscovery 與直接讀路徑都不用猜 | FR-01, FR-04, D3, D28 |
| D20 修 | 全域視圖 = Grafana（Prometheus 抓各 Node）；§15 十題的驗收與 Prometheus 不可用時 = CLI 扇出 `GET /status`。CLI 為動作入口與驗收基準，因其不依賴 Prometheus（AC-CP-03） | §15, AC-CP-03, D27 |
| D23 修 | Application 整合契約新增：Application 必須開 Actuator `/actuator/prometheus` 供抓取，library 指標帶 `{node, app_instance}`；這是 DG-02 唯一的量測來源。library 設定新增 Policy fallback 檔路徑（預設 `<app data dir>/gigaxfer-policy.json`） | DG-02, D18, D27 |
| D27 修 | Prometheus 與 Grafana 放公司現有監控基礎設施，每 Fab 一組；資料保留 90 天；告警規則進 repo 由 Alertmanager 評估；Alertmanager watchdog（永遠 firing）送既有 NMS 作死人開關，NMS 另收事故級告警。Trust 軸告警只用 `up{node}`，不用 `absent()`（新 Node 無義務時會誤報） | §11, D27, D33 |
| D30 修 | UNREACHABLE 改為連續 12 個輪詢間隔（60 s）無 pending，避開 D34 重啟預算。Source 端 `/file` 最大並發連線 16（Tomcat 設定，超過者在 accept queue 排隊，Target 只看到慢不收錯誤，不違反 D12a 不做流控），為 operational policy。容量門檻預設 reject 10 TB、alert 20 TB，NAS 容量遠大於此，本版不做容量規劃 | D12a, D21, D31, D34 |
| D33 修 | ops 下 pause(B) 時該 Target 的義務帶 `paused` 標籤；警告級告警（age > 2 h、unknown 持續）排除 paused pair，事故級（age > 12 h、integrity > 0）永不排除。計畫性 PM 前 ops 必須下 pause 才不吵，與 D22 一致 | DG-04, D22, D33 |
| D34 修 | systemd 只看 process 存活，不打 health。`/health` 對外回報 DB 與 NFS 狀態當 readiness 指標（Availability 軸第二層）。啟動時 DB 連不上：留在 process 內重試、health 回 DOWN、不 exit，避免 NFS hang 或 DB 短暫不可用觸發無效重啟迴圈 | DG-05, D34 |
| D37 | sync 主機冷備：每 Node 預備第二台主機，NFS mount、Oracle 連線與 systemd unit 設好，CD 同步送 config，但 process 不啟動。原機硬體死亡由 ops 手動在備機啟動，紅線 = 先確認原機 process 已死（維持 D9 單一寫入者）。不做自動 failover（需 fencing，複雜度不值）。恢復時間 = ops 反應時間 + 30 s；期間該 Node 作為 Source 無人服務 `/pending`、作為 Target 無人拉，lag 漲但義務不丟 | DG-05, D5, D9, D34, T07 |
| D38 | 技術棧：sync service 與 library 皆為 Java + Spring Boot；sync service 用 Spring Boot 內嵌 HTTP 伺服器（預設 Tomcat）、JPA（D9）、Actuator（D27）。D30 修的「`/file` 並發連線 16」即內嵌伺服器的連線上限設定 | D9, D17, D23, D30 修 |
| D15 修 / D23 修 | Consumer 查詢改走本 Node sync service `GET /locate/<identity>?dataClass=<class>`：Source 角色查 `file_identity`、Target 角色查 `received`，回 {state, path}；無列時以 Policy (source, class) 是否含本 Node 判 DATA_NOT_READY / NOT_EXPECTED。library 介面改為 `exists(identity, dataClass)`、`read(identity, dataClass)`，快取 READY 結果；sync service 不可達且無快取 → UNAVAILABLE（SR-06 允許）。理由：D2 路徑含日期而 identity 無日期、Policy 需 Data class 而 identity 無 class，原 D23「一次 stat 不碰服務」做不到；read 不在 Local Transaction SLO（Q9），本 Node sync service 非 Remote Node 亦非 CP | SR-06, D2, D15, D23, T28 |
| D16 修 / D19 修 | Target 端遺失與損壞偵測改為 Target 自查、只回報異常：Shallow（每 6 h）對 `received` 中 published_at 在 D 天內的列 stat `<key>` 比 size，缺或不符 → `POST /report LOST`；Deep（滾動，7 天一輪）重算 digest 與 `received.digest`（來自 manifest 的基準）比對，不符 → `POST /report CORRUPT`；不再回報 VERIFIED。覆蓋率由 Target 曝 `shallow_check_coverage`、`deep_check_coverage` metric。Source 收 LOST / CORRUPT → 重開 PENDING 保留歷史。Source 端 Shallow ② 的 `/received?since` 只做「Target 有但 Source 未完成 → 補 COMPLETED」與 digest 紀錄比對，不再宣稱能抓 Target 遺失。理由：增量拉取抓不到缺席；45 次/s 的 VERIFIED 是為不存在的需求付費 | RC-02, RC-03, RC-04, IR-03, T30 |
| D39 | Target 重拉時發現 `<key>` 已存在且 digest ≠ manifest → rename 為 `<key>.corrupt.<ts>` 留證據，再 link 新檔；證據檔由清道夫（D35）30 天後刪。`evict` 保留給 ops 處理 QUARANTINED。理由：manifest 為不可變權威，Target 副本不同即 Target 損壞，RC-03 要求自動修、IR-03 要求留證據 | RC-03, IR-03, D25, D35, T30 |
| D29 修 | `rebuild` 維持單一動作；`rebuild_in_progress` 期間 `/received` 與 `/locate` 回 503，Source 依 D19 標 unknown 保留上次結果，Consumer 得 UNAVAILABLE；`received` 由本地 NAS 掃描重算 digest 重建完才解除 | D19, D15 修, T18 |
| D40 | `GET /file/<identity>?target=<self>`：Source 查該 identity 對該 target 是否存在義務，無則 403。Policy 從建議變強制，Fab 內 shared token 不再等於任意讀取 | DG-04, D14 |
| D41 | Target 端拉取排程：每個 Source 一個佇列，4 個全域槽 round-robin 輪流取，避免單一 Source 的 backlog 餓死其他 Source | DG-01, RR-05, D30 |
| D42 | 義務持久化狀態只有三種：PENDING、QUARANTINED、COMPLETED。其餘推導：RETRY_WAIT = PENDING 且 next_attempt_at > now；IN_PROGRESS = Source 記憶體中「上一輪交給該 Target 的集合」（重啟即失，只影響顯示，不影響義務保存與重交）；BLOCKED = per-Target 條件推導：paused 存 `target_control(target_node, paused, paused_by, paused_at, reason)` 並持久化，`/pending` 以 WHERE target 未 paused 過濾，新增義務自然被涵蓋；unreachable 由記憶體 liveness（last_pending_at）推導；capacity 見 D43。刪除 `obligation.last_handed_at`、`obligation.paused`、`target_liveness` 表。告警排除用 `paused{source,target}` gauge。spec §14 七種狀態為 `/status` 顯示對照 | §14, §17, D12a, D22, D24, D33 修, T17, T31 |
| D43 | Target 端共用儲存閘門：任一 worker 遇到儲存類失敗（statfs / write / fsync / link timeout 或錯誤、容量低於門檻）立即關閉閘門，所有 worker 取下一筆前檢查；關閉後只剩當時最多 4 筆執行中工作可能失敗並 report FAILED。恢復：每輪 statfs 成功 → 半開，只放一個 worker 拉一筆真實工作，成功 → 全開，失敗 → 關閉。不另寫探測檔。不再回報 CAPACITY，`/report` kind = DONE / FAILED / LOST / CORRUPT；FAILED 只用於單檔問題。Source 只見 staleness → UNREACHABLE；原因由 Target 自己的 `storage_health{node}`、`capacity_alert_status{node}` 呈現，帶最後更新時間，過期顯示 unknown。這是 Target 對自己 NAS 的閘門，不是跨 Node 流控，不牴觸 D12a | RR-04, RT-02, SR-05, D12a, D21, T12, T21 |
| D44 | manifest 原子建立：寫 `<key>.manifest.<uuid>.tmp` + fsync → `link(tmp, <key>.manifest)` 原子宣告（EEXIST → 讀既有 manifest 必為完整，比 digest）→ unlink tmp。刪除 D3a「半截 manifest → unlink + 重建」規則：該規則在兩個寫者並發時會把正常寫入中的 manifest 當半截刪掉，留下 A 的內容配 B 的 manifest。link `<key>` 得 EEXIST 時仍重讀 `<key>` 比 digest 後才回 SUCCESS，這是重試路徑當下的結果確認，不刪。代價每筆多 1 link + 1 unlink | FR-02, FR-04, SR-04, D3, D3a, T20, T29 |
| D45 | 設定不熱載入：設定物件啟動時讀一次、process 內不可變；啟用 = 驗證 → rename → 重啟。流程：CD 放 candidate 後 `systemctl restart`；sync service 啟動時若有 candidate → 驗證（schema、版本遞增、Policy 段與 active 相同）→ active→lkg、candidate→active → 載入並開始服務後才曝新 `active_config_version`；驗證失敗留 candidate、計 `activation_failure_count`、用 active；active 載入失敗用 lkg 並計失敗。每次設定變更代價 30 s lag | §13, AC-CFG-01～05, D17, D34, T15, T26 |
| D46 | 三句不變量：Source 掃描器不修改 NAS；Source 正式 `<key>` 只由 library 發布；清道夫僅依 Abandoned 規則清理暫存與未發布 metadata。rediscovery 純唯讀：manifest + `<key>` = Ready；manifest + `.writing` 無 `<key>` = Writing，Application 重呼 finalize 由 library 補 link，超 TTL 由清道夫當 Abandoned。刪 D8「重跑交易前先 exists」：重跑直接 beginWrite + finalize，冪等保證正確，重寫 MB 級檔為可接受浪費。`/locate` 對 source == 本 Node 的查詢：有列 READY，無列 DATA_NOT_READY（掃描 15 s 內出現），永不回 NOT_EXPECTED | FR-02, D3, D8, D15 修, D35, T18, T29 |
| D47 | 取代 D39：不留 `.corrupt` 檔。修復順序：Target 下載並驗證新副本到 `.writing` →（證據已在 Source 持久化：Source 只在持久化 LOST / CORRUPT 後才重交，Target 能重拉即證據已落地）→ `<key>` 存在且 digest ≠ manifest → `rename(.writing, <key>)` 原子覆蓋，無「`<key>` 短暫不存在」窗口，中途 crash 只剩「還沒換」→ 更新 received 列 → report DONE。正常路徑仍用 link 取 EEXIST。證據 = LOST / CORRUPT 回報的 identity、Source、Target、expected / observed、時間，存於 Source obligation 歷史；放棄壞檔原文鑑識 | RC-03, IR-03, D12, D16 修, D35, T30 |
| 簡化審查 2 | 本輪否決的簡化：刪 `/received`（spec RC-02 要求比對雙方 Integrity baseline 紀錄，`/pending` 重交只能補 DONE）；刪 link EEXIST 時重讀 `<key>`（重試路徑的 SUCCESS 需要當下證據） | RC-02, SR-04 |
| D24 修 | schema 依 D42、D47 調整：`obligation.state` 只有 PENDING / QUARANTINED / COMPLETED，刪 `last_handed_at`、`paused`；刪 `target_liveness`（改記憶體）；新增 `target_control(target_node, paused, paused_by, paused_at, reason)` 與 `obligation_history(identity_ref, target_node, kind ∈ {LOST, CORRUPT, REOPEN, RELEASE}, expected_digest, observed_digest, at)` 作 IR-03 證據 | D24, D42, D47, IR-03 |
| D48 | 正式位置由 identity 決定：manifest 搬到 `<source>/<ns>/<bucket = hash(key) 前 3 hex>/<key>.manifest`（4096 桶，30 天約 730 個/桶），它本來就是權威（D1），只是換位置，不是新索引。內容檔留在 `<source>/<ns>/<class>/<date>/<HH>/<key>`，位置由第一個成功宣告 manifest 的人決定並寫入 manifest 的 `content_path`；Finalize 第③步永遠 link 到 manifest 記的 content_path，不用當下時間。跨小時重試：manifest link EEXIST → 讀 content_path → link → EEXIST → 驗 digest → SUCCESS；不同內容 → CONFLICT。掃描不變：掃小時目錄找 `<key>`，由路徑推 identity 去桶讀 manifest。Target 鏡像 content_path，不寫 manifest。清道夫新增：content_path 的 `<key>` 已被 NAS 清掉且 manifest mtime 超過保留期 → 刪 manifest。理由：D2 路徑含 Finalize 小時，跨小時重跑無 EEXIST，D46 的冪等與 FR-04 衝突檢查都失效 | FR-01, FR-04, D2, D3, D44, D46, T20, T29 |
| D49 | Target 完成證據三個補正：① Target 回報 LOST / CORRUPT 前先刪自己的 received 列，重交才不會被「received 已有 → DONE」短路；② Target rebuild 永不從檔案算基準：向每個 Source 拉 `GET /pending?target=<self>&state=COMPLETED&cursor=` 取 manifest 欄位，stat + size 相符即寫 received 並帶 Source 的 digest，Source 不可達的部分維持 unknown、`/locate` 回 UNAVAILABLE，digest 由 Deep check 七天一輪驗；③ 取代 D47 的推論：`<key>` EEXIST 且 digest ≠ manifest → 先 `POST /report CORRUPT` 拿到 200 → 才 rename 覆蓋，回報失敗放棄本次。事件順序：obligation 加 `epoch`（重開、release 時 +1），`/pending` 交付與所有 `/report` 帶 epoch，Source 只接受當前 epoch，延遲的舊 DONE 失效 | RR-03, RC-03, IR-01, IR-03, D29 修, D47, T19, T30 |
| D50 | 掃描範圍：rebuild 與每 6 h 的 Source 全量對帳掃都覆蓋整個 NAS 保留期（30 天），逐小時目錄 readdir 對 DB 該小時集合，720 個目錄每 6 h 一輪，不 stat。D10 的 D 參數 = 保留期。七天只剩兩個用途：Target Shallow 自查的快速窗口、Deep check 一輪週期。理由：第 1 天資料第 2 天 QUARANTINED 第 10 天 DB 遺失，七天 rebuild 會讓未完成義務消失；效能策略不得改變資料責任 | RC-01, RC-02, RR-02, D10, D19, D29, T18 |
| D51 | NFS 隔離改為有界執行器：library 與 sync service 各一個固定大小 pool（operational policy，預設 library 16、sync 32），槽位只在 syscall 真的回來才釋放（hard mount 下卡住的 thread 殺不掉，timeout 只是呼叫者不等），滿了立即拒絕：beginWrite 回 FAILURE(UNAVAILABLE)、finalize 中途回 PENDING_CONFIRMATION、Target worker 視同儲存閘門關閉；曝 `nfs_pool_exhausted_count`。舊操作恢復後：Source 端靠 uuid 檔名與 link 語意天然安全；Target `.writing` 改為 `<key>.<uuid>.writing`（撤回 D28 固定名），孤兒由清道夫既有 mtime 規則清 | SR-05, D3b, D23, D28, D43, T25 |
| D52 | 共用 NAS 的固定頻寬預算（皆 operational policy）：Deep check 讀取 20 MB/s、Target 拉取總量 50 MB/s、Source 對外 `/file` 總量 100 MB/s；本地交易優先靠預算與合併負載壓力測試證明，不做自適應控制。七天巡檢與二十四小時追平降為「待驗證目標」，Deep 一輪超過七天告警。撤回 D30 修的「`/file` 連線上限 16」：Target 並發 4 × ≤9 Target = 最多 36 條，上限由結構給定，需要的是 bytes/s 預算。修正 D31：sync 主機單獨停機時 App 照寫，Source 恢復後九個 Target 同時追，Source 供檔預算即為此而設 | DG-01, DG-02, RR-05, D30 修, D31, D38, T16, T24 |
| D9 註 | 無共用 Oracle 開發庫可自筆電連線，維持 H2 Oracle mode 與 SQL 可攜；單元測試盡量不碰 DB，dialect 漂移由 CI 的 Oracle Free 兜底 | D9 |
| D37 修 | sync 主機死亡的恢復目標 15 分鐘，含偵測、值班反應、備機啟動。保留每 Node 專屬冷備；`up{node}` 消失 > 3 個抓取週期升為事故級（立即 page），啟動前 fencing 清單（確認原機 process 已死：ILO 斷電或 console 確認）；驗收測試 T07 變體量測含值班的端到端時間。§18 新增列「sync 主機恢復 ≤ 15 min」 | DG-05, §18, D37, T07 |
| D53 | manifest 本版不刪：不自動刪、不提供 release-claim（ops 無法保證 App 主機上卡住的舊 link 不會再回來，審計擋不住交錯）。撤回 D48 的清道夫 manifest 規則與 D11 / D35 對 manifest 的 Abandoned 刪除；清道夫只刪 `.writing` 與 manifest tmp。Abandoned 宣告若之後被同 key 不同內容重寫 = SR-01 的 App 違約，走 FR-04 CONFLICT，App 改用新 key。三條掃描規則：① manifest EEXIST 比 identity、Data class、size、digest 四項，同者「允許續行到第③步」，link 成功或 EEXIST 驗證通過才 SUCCESS；② 掃描找到 `<key>` 須確認路徑 == manifest.content_path，不等即不算 Ready、計 anomaly；③ 全量對帳同時枚舉 manifest 桶：manifest 有內容無 → DB 曾 Ready → Source 端遺失，義務未完成即 unrecoverable；DB 無紀錄 → 「未發布或發布後遺失」候選，計 unknown，不斷定 Abandoned；DB 剛重建 → unknown。只有不在 DB 的 manifest 才讀內容。累積量 10⁵/日、每桶每年 8.8k 項，readdir 與 metadata 成本列入壓測 | FR-02, FR-04, RC-01, RC-02, RT-01, D11, D35, D48, T11, T20 |
| D54 | 取代 D49 ①：Target 發現異常（自查或拉取時 EEXIST 不符）在同一筆 Target DB 交易 upsert received 列為 invalid，記 invalid_reason、observed_digest、一次性 event_id、發現時的 (incarnation, epoch)、report_pending = true；背景重送 report 直到 ACK。ACK 只清同一 event_id 的 report_pending；ACK 不把列改回 valid，valid 只由修復流程的發布動作寫回。`/locate` 對 invalid 列不回 READY；快速路徑「received 已有 → DONE」只認 valid 列 | RC-03, IR-03, RR-03, D49, T19, T30 |
| D55 | 代次協定，取代 D49 ③：(1) 任何新 event_id 的 LOST / CORRUPT，Source 同一交易寫 obligation_history、epoch + 1、state → PENDING，不論原本是 COMPLETED 或 PENDING（否則延遲的舊 DONE 會在 CORRUPT 之後通過），ACK{ACCEPTED, epoch}；同 event_id 重送不加代，ACK{DUPLICATE, epoch}；incarnation 不符或 identity 不存在 ACK{STALE}。(2) DONE(e) = `UPDATE ... WHERE epoch = e AND state = PENDING`，失敗即 STALE。(3) 代次只從 `/pending` 交付取得，ACK 不授權舊工作換代；Target 發現異常後刪 `.writing`、釋放 in-flight，不就地替換。(4) Target 收到交付 (inc, e)，查 received 列 (inc′, r)：inc′ ≠ inc → 無可信列，走 link 路徑，EEXIST 驗既有檔；r < e → 即修復授權，下載驗證後 `rename` 覆蓋，received 寫回 valid 與 (inc, e)，DONE(e)；r = e 且 valid → 直接 DONE；無列 → link 路徑。(5) `node_meta` 一列存 incarnation UUID，rebuild 時換新，交付與所有 report 皆帶。(6) Target 自查跳過 in-flight 集合裡的 identity。修復代價 = 多等一輪輪詢加一次 MB 級重新下載 | RR-03, RC-03, IR-02, IR-03, D29 修, D49, T09, T19, T30 |
| D19 修 2 | Target Shallow 自查每 6 h 覆蓋保留期內全部 received 列，不限七天：逐內容小時目錄 readdir 對 received 該小時集合，存在性靠名字，size 靠 readdir 後的屬性快取（NFS client READDIRPLUS）。壓測若太重拉長到 24 h，不縮範圍。七天只剩 Deep 的候選週期。理由：第十天 Target 檔被刪，Source 全掃看不到、七天窗口跳過 | RC-02, D19 修, D50, T30 |
| D52 修 | 撤回「Deep 一輪超過七天告警」：27 TB 在 20 MB/s 下約 16 天，七天告警會常駐觸發。Deep 週期為量測值 `deep_check_cycle_days`，20 MB/s 為壓測起點；壓測後在提高預算與接受較長偵測期限之間選，T30 偵測時限 = 選定後的週期，屆時才設告警。七天不是承諾 | RC-02, IR-03, §18, D52, T30 |
| D51 修 | 補三句：執行器無佇列，滿即拒；timeout 不釋放未結束操作的所有權；清道夫跳過 sync service 自己 in-flight 集合裡的 uuid 暫存檔，library 端暫存檔超 TTL 被刪最壞是 link 失敗回 FAILURE，不壞資料。sync service 的 NFS pool 分兩個：`/file` 供檔專用 32、本地工作（掃描、發布、自查）16，串流不餓死本地工作 | SR-05, D35, D51 |
| D30 修 3 | `/file` 專用有界工作預算：semaphore 預設 48，滿了立即 503，Target 視為稍後再試、不記 FAILED、不計 attempts；與 D52 的 bytes/s 預算各管一件事（工作數管卡住時的占用，bytes/s 管吞吐）。撤回 D52 中「不另設上限」的說法：4 × 9 = 36 是客戶端正常活動數，Target timeout 後重發時 Source 舊請求仍在 | RR-04, SR-05, D52, T16 |
| D55 修 | ACK 契約寫死：重送回 `{DUPLICATE, accepted_epoch, current_epoch}`，accepted_epoch 取自 obligation_history 該 event 的 epoch_after；ACK 任何欄位不得用於 DONE，DONE 的代次只來自 `/pending` 交付。`/received` 每列回傳 incarnation、epoch、valid；Source 補 COMPLETED 一律 `WHERE incarnation = ? AND epoch = ? AND state = PENDING` 且只認 valid 列。第 (1) 條「不論原狀態皆加代」維持，延遲的舊 DONE 在 CORRUPT 之後不會通過 | RR-03, D55, T19 |
| D19 修 3 | Target Shallow 的正確性不依賴快取：枚舉名字並讀取必要屬性（size）；READDIRPLUS 與屬性快取只降低 RPC 成本；屬性讀不到即 unknown，不算通過。週期若改 24 h，對應檢查期限與告警同步調整 | RC-02, RC-04, D19 修 2 |
| D52 修 2 | 兩個量分開：`deep_check_cycle_days` 是觀測值；T30 偵測期限是 operational policy 常數，壓測後人工設定；告警 = 觀測值 > 常數；常數不因觀測值變慢而自動放寬 | IR-03, §18, D52 修 |
| D30 修 4 | `/file` 503 後 Target 對該 Source 跳過本輪，下一輪輪詢再試，不立即重送；semaphore 只在工作真正結束時釋放，HTTP timeout 不是釋放依據；48 / 32 / 16 為起始值，壓測驗證拒絕率與控制端點延遲 | RR-04, SR-05, D30 修 3, D51 修 |
| D51 修 2 | library 暫存檔被清道夫依 TTL 刪除後的結果判定：link 尚未送出且來源已不存在 → FAILURE；link 已送出而結果未明 → PENDING_CONFIRMATION，重呼 finalize 走 rediscovery 查證；不得因暫存檔消失推論正式檔未發布。契約明寫「暫存寫入可能因超 TTL 被中止」 | SR-04, FR-02, D3a, D51 修, T29 |
| D29 修 2 | Source rebuild 五步：隔離 → 換 incarnation → 清可推導資料 → 全掃 → 匯入歷史證據。隔離：Node 間端點（`/pending`、`/report`、`/received`、`/locate`）回 503、停排程（增量掃描、對帳、自查、清道夫）、拒絕 ops 寫入類動作、等所有可能回寫狀態的工作結束（含已啟動的排程、背景回報處理、未決操作，不只 HTTP handler）；`rebuild_in_progress` 與新 incarnation 同交易寫入 `node_meta`，重啟見旗標即維持封鎖並從清表重做，不斷點續傳。清表範圍只有 `obligation` 非 QUARANTINED 列與 `inspection`；`file_identity` 由掃描 upsert（不符即 conflict）、`received` 不清；保留 QUARANTINED 及其 identity、invalid received（含 report_pending 直到 ACK）、`obligation_history`、`ops_audit`、`target_control`，留下的不需重套。Source 角色：對每 Target 拉 `/received`，valid 且 identity + size + digest 相符 → COMPLETED(inc, 1)，不比列上的舊代次、不碰 QUARANTINED；Target 503 的部分留 PENDING 靠交付換錨。Target 角色：依 D49 ② 向各 Source 取 COMPLETED 基準只補缺列，既有 valid 列與驗證時間保留，不可達範圍維持 unknown。COMPLETED 三個入口只有此匯入不查代次，`/report DONE` 與對帳 ② 一律查 (incarnation, epoch)，延遲抵達的舊 `/received` 回應也不繞過。期間該 Node 同步暫停，App 寫入不受影響，恢復後由增量掃描與對帳補登記；完成時間由全量重建測試量測，撤回「小時級」估算。DB 從備份還原視同遺失，必走 rebuild 換 incarnation | RR-02, RR-03, IR-03, D29 修, D49, D55 修, T18 |
| D55 修 2 | 交付 (inc, e) 對 received 列 (inc′, r, valid) 的分支寫滿，取代 D55 (4)。同 inc：r = e 且 valid → DONE；r = e 且 invalid → 跳過本次交付，不下載、不送 DONE、不建事件，既有事件 X 的背景重送照常，等 X 被接受與下一次有效交付，期間義務未完成、列保持 invalid；r < e → 修復（下載驗證後 rename 覆蓋，寫回 valid 與 (inc, e)）；r > e → 過期交付（佇列中的舊交付在修復後才輪到），跳過、不改列，記診斷計數不算 identity_conflict，連續多輪同一列較低代次才告警疑似 Source 回退。inc′ ≠ inc：valid 且 identity + size + digest 與交付一致 → 換錨，列改 (inc, e) 直接 DONE 不讀檔、不重設 Deep 驗證時間；invalid 或不符 → 修復分支。無列 → link 路徑。不變量：同 incarnation 下，已 invalid 的 received 只由更高代次的修復交付恢復 valid。Source 端：DONE 條件更新加 incarnation；舊 incarnation 的 LOST / CORRUPT 在義務存在、event_id 去重、非 QUARANTINED 下照常加代重交付（歷史若隨 DB 遺失，舊事件第一次當新事件加代一次，代價一次重交付）；義務不存在 → STALE{OBLIGATION_NOT_FOUND}；QUARANTINED 只記歷史不重開，release 解除隔離、resume 解除暫停，符合交付條件後才收到新 incarnation 的工作 | RR-03, RC-03, IR-03, D55, D55 修, D29 修 2, T19, T30 |
| D54 修 | 每個 invalid 列保留一個目前異常事件 X：ACK 結束回報重送，修復完成才結束異常。首次建立 X 必須是原子狀態轉換：無列時原子 INSERT invalid 與 X；有 valid 列時 `WHERE valid = true` 條件更新；已有 invalid 列則沿用 X，不覆蓋 event_id；並發插入衝突後重讀既有列（Shallow、Deep、拉取路徑 EEXIST 皆同）。自查略過 invalid 列，列為已知異常，`received_invalid_count` 與本輪實際檢查覆蓋率分開呈現，略過的不算已檢查。ACK ACCEPTED / DUPLICATE 只清同一 event_id 的 report_pending；STALE{OBLIGATION_NOT_FOUND} 保持 invalid 與 report_pending，有上限指數退避重送，不增加傳輸 attempts，Source 之後補建義務即為停止條件（清掉會與 D55 修 2 的「跳過等 X」互鎖）。跨 incarnation 的覆寫授權依 D55 修 2 換錨規則，不由「跳過 invalid」授權 | RC-03, IR-03, D54, D55 修 2, T19, T30 |
| 協定審查 3 | 階段結論：本輪協定分支（rebuild 隔離與匯入、交付分支表、單一異常事件、STALE 退避）已形成明確規則與故障驗證清單（system-design §11 情境 1～8 含分支），尚待實作測試確認。停止發散式 grilling，改為情境驗證與壓測。D56 的時間起點、到期與 unrecoverable 語意獨立待決，不納入本輪 | 過程註記 |
| D56（待決） | identity 保留與搜尋時窗分離，尚未定案。起因：D35 與 D53 之間漏掉一個狀態：正常完成的資料內容依 NAS 保留期清除、DB 列依 D35 在保留期 + 30 天 purge、manifest 依 D53 永久留下，D53 ③ 會把它當「manifest 有、內容無、DB 無」每輪讀內容並計 unknown，unknown 告警常駐、讀取量隨歷史線性成長。已同意的原則（2026-09-22 審查）：① 三十天只是依 NAS 保留設定的搜尋時窗，不是義務到期日；窗外 PENDING 照退避與 pause 進 /pending，QUARANTINED 保留追蹤不自動交付，backlog 與 age 照算；不再稱「責任範圍」。② 業務時間 = source_ready_at；manifest mtime 只作桶枚舉粗篩，不得據以推論義務狀態，不宣稱 NAS 時鐘誤差上限，粗篩仍需取得 mtime、桶枚舉與 metadata 成本隨歷史成長。③ unrecoverable 只由觀察觸發：已知曾 Ready、義務未完成、對 content_path 明確得到不存在；timeout / 權限 / 讀取失敗不算不存在（RC-02）；/file 補 410，狀態與證據 commit 後才回，Target 不送 FAILED 不加 attempts，更新沿用 D55 的狀態保護。已採納：received 加 source_ready_at（D35 對 received 引用的 source_ready_at 原不在 D24 修 schema）。定案前須接上兩個缺口：(a) D35 按時間 purge 沒有狀態例外，第 60 天會刪掉窗外未完成義務；Source Shallow 須涵蓋窗內發現集合 + DB 全部未完成義務，窗外者直接檢查 content_path，只靠下一次 /file 抓不到長期 paused 或 QUARANTINED 的 Source 遺失。(b) 窗外遲到發布沒有發現路徑：D53 ① 允許既有 manifest 四項相同後續行第③步並沿用原 content_path，第 1 天宣告、第 31 天 link 成功時 manifest mtime 與內容目錄都在窗外、DB 無列，/pending 與 /file 都接不到；DB 重建：D29 修 2 清除非 QUARANTINED 的 obligation 但保留 file_identity，窗外 PENDING 不能只靠 30 天 NAS 掃描重建，須明定如何從保留的 file_identity 重建。F21 一併改為「已知曾 Ready 的正式內容遺失且義務未完成」，manifest 消失不等於內容遺失；/file 對內容不存在的回應目前未明定，404 → 反覆 4xx → QUARANTINED 只是可能落入的通用路徑。已刪除的推論：「範圍內 DB 列必然存在」不成立 | RT-01, RC-02, DG-04, D19 修 2, D32, D35, D50, D53 |
| D56 定案 | identity 保留與搜尋時窗分離，取代 D56（待決）。① 30 天是依 NAS 保留設定的搜尋時窗，不是義務到期日：非 COMPLETED 不按時間刪，窗外 PENDING 照退避與 pause 進 `/pending`，QUARANTINED 保留追蹤不自動交付，backlog 計全部。② purge 以 identity 為單位：該 identity 所有義務皆 COMPLETED 且 source_ready_at 早於窗 + 30 天才整組刪 file_identity / obligation / received；任一未完成即整組留。③ Source Shallow = 窗內內容目錄差集 + DB 全部未完成義務（含窗外）直接 stat content_path；業務時間 = source_ready_at，manifest 桶枚舉以 mtime 粗篩窗內，窗外 manifest 不枚舉；只有明確得到「不存在」才 unrecoverable，timeout / 權限 / 讀取失敗計 unknown（RC-02）；`/file` 對內容不存在回 410，狀態與證據 commit 後才回，Target 不送 FAILED 不加 attempts。unrecoverable 的持久化形式 = 義務轉 QUARANTINED{SOURCE_LOST} 並寫一筆 `obligation_history{UNRECOVERABLE}`，以 (義務, kind) 去重只寫一次。④ 遲到發布不做 `.late`，用兩個邊界封閉：content_path 於第②步宣告時刻選定（D48 修）；續行順序四項比對 → rediscovery → 年齡 → link，年齡只約束再次嘗試發布，N = 7 天（D53 修）；已送出的 link 依 D51 修 2 查證，不得推定 ENOENT（NFSv3 LINK 用來源 file handle）。預算：固定項 N 7 + TTL 1 + 清道夫間隔 1 + 時鐘裕度 1 + 對帳週期 0.25 = 10.25 天；清道夫停跑（sync service 停機、rebuild 隔離、NAS 中斷）合計 < 19 天（operational policy，上限 19.75），以 `cleanup_last_success_time` 計算，清道夫依賴 NAS 故中斷期間不會成功；超過即失去發現保證並告警。mtime 用於拒絕發布後承擔時間界線，裕度以天計。⑤ 恢復限制：整庫遺失、或備份之後新增而還原時已在窗外的 identity，其未完成義務無法重建，連 unknown 都不顯示；DB 備份為 ops 責任，還原後仍 rebuild 換 incarnation。⑥ 未完成義務永久追蹤，QUARANTINED 累積無上限；release / evict / App 換 key 都不結清 Source 已遺失的義務，只標 unrecoverable 留證據 | RT-01, RC-02, DG-04, D11, D19 修 2, D32, D35, D50, D53, D55 |
| D29 修 3 | rebuild 建列只補缺列，不清 obligation：既有 COMPLETED / QUARANTINED / PENDING 一律保留（否則 identity 因他 Target 未完成而留、本 Target 已 purge 的完成義務會被 Policy 展開復活成 PENDING，誤報 unrecoverable）。完成證據以條件更新既有 PENDING：對帳 ② 對 incarnation 不同的 Target 列，valid 且 identity + size + digest 相符 → `UPDATE … WHERE incarnation = 現行 AND epoch = 1 AND state = PENDING`，epoch = 1 即本命從未加代，加代過的不吃舊快照；隔離內把非 QUARANTINED 的 PENDING 列 epoch 重設為 1，隔離內跑一次，之後每輪 ② 自然補齊 503 的 Target。不引入 import_pending，保留 `rebuild_in_progress`。rebuild 重設每 Target 的 `/received` 游標為 0；503 或未完整讀完的一輪不推進游標。取消 D29 修 2 的清表範圍，其餘隔離規則不變 | RR-02, RR-03, D29 修 2, D55 修, D56 定案 |
| D48 修 | content_path 的小時目錄由第②步宣告時刻決定並寫入 manifest，不再由 beginWrite 的當下小時決定；`.writing` 留在 beginWrite 選的目錄不搬，第③步跨目錄 link（同一檔案系統）。beginWrite 建 `.writing` 所在目錄，② 另確保宣告時刻的正式目錄存在；link 次數不變，目錄操作最多多一次。理由：寫入橫跨多日再宣告時，正式位置仍須落在搜尋窗內 | FR-02, D3, D48, D56 定案 |
| D53 修 | ① 續行順序寫死：manifest EEXIST → 讀既有 manifest → 本次請求的 identity、class、size、digest 與之四項比對，任一不同 → CONFLICT → rediscovery `<key>`：存在且 digest = manifest → SUCCESS，不看年齡 → 不存在且需再次嘗試發布（重寫暫存或送出新 link）→ manifest mtime 超過 N = 7 天（operational policy）→ FAILURE{DECLARATION_EXPIRED}，App 換 key，與 CONFLICT 同結局；link 已送出結果未明 → PENDING_CONFIRMATION。年齡只約束再次嘗試發布，不推翻既有發布結果。D11 的 Abandoned 只忽略 `.writing`；Abandoned identity 的 `<key>` 之後出現即 ingest | FR-02, SR-01, SR-04, D11, D51 修 2, D53, D56 定案 |
| D35 修 | ③ purge 改 identity 整組規則（D56 ②），不再逐列按時間刪。曝 `cleanup_last_success_time{node}`：上次完整成功跑完三項職責的時間，啟動、部分完成、任一 NAS 操作失敗都不更新；清道夫停跑時間以它計入 D56 預算，> 19 天事故 | RT-01, D32, D35, D56 定案 |
| D33 修 2 | 事故告警一律以首次持久化的歷史事件觸發：unrecoverable、integrity_failure、identity_conflict 各在持久化 `obligation_history` 事件成功那一刻遞增 process 內 counter `incident_events_total{kind}`，Alertmanager `increase() > 0` 事故；同一 (義務, kind) 只寫一次，重掃、重啟、rebuild 不重寫歷史列故不重計；存量 `{kind}_count` 只上儀表板。Freshness 告警改用 `oldest_pending_age`（state = PENDING）：> 2 h 警告排除 paused、> 12 h 事故不排除；`oldest_unfinished_age`（全部非 COMPLETED）保留為原始指標只上儀表板；QUARANTINED 進入時已各自觸發事故，之後為人工佇列，其 age 另做面板 | §16, D33, D33 修, D56 定案 |
| D55 修 3 | 對帳 ② 讀 `/received` 時同時處理 invalid 列：其 event_id 不在 `obligation_history` → 視同收到該 LOST / CORRUPT 報告（寫歷史、epoch + 1、state → PENDING，不論原本 COMPLETED 或 PENDING；QUARANTINED 只記歷史、設 repair 留給 release）；已在歷史 → 不動作。封閉的缺口：Source 從備份還原成 COMPLETED，而 Target 的 invalid 事件已 ACK（report_pending = false）：Target 不再重送、Source 不交付，只有 ② 這條 Source 主動讀的路徑能救。rebuild 隔離內跑的那次 ② 同樣適用；Target 503 的部分由下輪 ② 補。冪等由 Source 交易內依 event_id 去重保證（`obligation_history` 對 event_id 唯一鍵），`/report` 與 ② 同時送入同一 X 也只處理一次 | RC-03, IR-03, D54 修, D55, D29 修 3, T18, T19 |
| D29 修 4 | Target 角色的 DB 從備份還原視同 rebuild：缺列依 D49 ② / F25 補建；既有 invalid 列不清，逐列重算 digest：符合基準 → 恢復 valid、清事件（Source 已修好而 Target 列過時）；不符或檔案缺 → 換新 event_id、report_pending = true 回報；既有 valid 列保留。封閉「Target 還原舊備份帶已 ACK 的 invalid 而 Source 已修復」與「備份後新增的列缺失」。valid 由 Target 自己重驗後恢復是此規則的唯一例外，前提是 Source 端該義務不在修復中，否則下輪交付仍以 r < e 覆蓋 | RC-03, RR-03, D29 修 2, D49, D54 修, T19 |
| 架構審查 4 | 評估「薄表 ＋ Source repair 授權」候選（保留 pull，Target 不比較代次，集中同步狀態於 Source）：日常交付規則 7 → 5 且 Target 不做代次排序；但災難匯入、事件 outbox、F25 都得保留，並新增「週期宣告 → ACK 裁決 → 重新觀察 → 換事件」流程，ACK 還須區分 COMPLETED / QUARANTINED，Target 還原後仍要 F25 補集合；淨簡化不明確，不採用，記為 system-design §12 備選。評估過程發現兩個現行設計缺口，以 D55 修 3、D29 修 4 封閉。central sync service 與 push 一併評估後關閉：本地交易不受影響，損失的是爆炸半徑與 ADR-0002 的隔離 | 過程註記 |
| D29 修 5 | 取代 D29 修 4 的 invalid 列恢復程序。Target DB 從備份還原視同 rebuild：缺列依 D49 ② / F25 補建；既有 invalid 列與其 report_pending 一律保留，不清、不換。程序：① 未 ACK 的事件先以既有 event_id 重送，經 Source 去重與 ACK 確認才算保存，Source 不可達就保留。② 同時滿足四條才自行恢復 valid：原事件已保存（ACCEPTED / DUPLICATE，或還原列本就 report_pending = false）、F25 拉到的 Source COMPLETED 證據含此 identity、基準（size、digest）相符、本地重算 digest 通過；恢復時列的 (incarnation, epoch) 取 Source 交付值。③ 原事件已保存、Source 為 COMPLETED、但本地重驗不符或檔案缺 → 換新 event_id 回報。④ Source 為 PENDING（修復中）、STALE、不可達，或原事件未保存 → 維持 invalid 與 unknown，不動；「沒收到交付」不是「不在修復中」的證據，可能只是 paused、退避或不可達 | RC-03, RR-03, IR-03, D29 修 4, D49, D54 修, T19 |
| D29 修 6 | 取代 D29 修 5 的恢復程序，兩個修正：恢復檢查持久化並重試；「已保存」只由本次確認。Target DB 從備份還原視同 rebuild：缺列依 D49 ② / F25 補建；既有 invalid 列全部標 `received.recovery_pending = true`（持久化，重啟不丟），report_pending 保留。恢復檢查為背景工作，每個自查週期對所有 recovery_pending 列重跑直到旗標清除：① 不論備份中 report_pending 為何，一律以原 event_id 重送 X：ACCEPTED → Source 原本沒有 X，現已保存並加代，維持 invalid 等修復交付，清旗標；DUPLICATE → X 已保存，續行 ②；STALE 或不可達 → 保留旗標，下輪再試。② 以 `/pending?state=COMPLETED&identity=` 向 Source 取該 identity 的 COMPLETED 證據：有且基準相符 → 本地重算 digest：通過 → valid、(incarnation, epoch) 取 Source 值、清事件與旗標；不符或缺 → 換新 event_id 回報，清旗標；無（PENDING、QUARANTINED 或不存在）→ 維持 invalid，保留旗標下輪再試，修復交付若先到則以 r < e 覆蓋並清旗標；不可達 → 保留旗標。備份裡的 report_pending = false 不作為「已保存」的證據 | RC-03, RR-03, IR-03, D29 修 5, D49, D54 修, T19 |
| D29 修 7 | D29 修 6 補三處，其餘不變。(1) 缺列續行：F25 補建以每個 Source 一個持久化游標追蹤（`rebuild_progress{source_node, cursor, done}`），背景推進到該 Source 回傳結尾才 done；503、未完整翻頁、重啟都從游標續；任一 Source 未 done 期間，自查覆蓋率不得宣稱完整，`/locate` 對該 Source 的無列 identity 回 UNAVAILABLE 而非 DATA_NOT_READY / NOT_EXPECTED。(2) 跨代證據一致：ACK 三種皆帶 Source incarnation；恢復檢查 ① 得 DUPLICATE 記下 incarnation，② 的 COMPLETED 證據（列本就帶 incarnation）必須同一 incarnation，否則重做 ①；中途換 incarnation 即 Source 曾重建，X 可能已失，只有重送能補回。(3) 舊工作回寫保護：恢復檢查對該 identity 取 in-flight 排他，且所有回寫皆條件更新 `WHERE event_id = X AND recovery_pending = true AND valid = false`（恢復 valid、換新事件、清旗標皆同），條件不符即丟棄本次結果；正常修復先完成再產生 Y 時，舊工作清不掉 Y | RC-03, RR-03, IR-03, D29 修 6, D49, D54 修, D55 修, T19 |
| D29 修 8 | D29 修 7 的補建游標補四處。(1) 游標效期：`rebuild_progress{source_node, incarnation, as_of, cursor, done}`；普通重啟接續；再次從備份還原則全部重設；游標綁 Source incarnation，回應（含空頁）皆帶 incarnation，不同即該 Source 重新枚舉，已補列保留。(2) 資料先於進度：本頁 upsert 與游標推進同一交易提交，允許重播；本地 stat 明確 ENOENT → 建 invalid 列 ＋ LOST 事件回報；stat 其他失敗（timeout、EIO、權限）→ 本頁不提交、游標不推進、下輪重試同頁，不得跳過後宣告 done。(3) 分頁定義：Node 間所有列舉（`/pending?state=COMPLETED`、`/received?since=`）皆用穩定排序 keyset 分頁，鍵 = (時間戳, identity)，時間戳為 Source 的 completed_at 或 Target 的 published_at；截止範圍 as_of = 補建開始時刻（該 Source 時鐘），只枚舉 ≤ as_of 的列，之後完成的由正常交付建列；不用 OFFSET。done = 該 incarnation 下 ≤ as_of 的 keyset 走到結尾。(4) `/locate` 判定順序：Policy 先，Policy 不要求此 Target → NOT_EXPECTED，與補建進度無關；Policy 要求且無列且該 Source 未 done → UNAVAILABLE；其餘同 D15 修 | RC-02, RC-04, RR-03, D29 修 7, D19, D15 修, T19 |
| D29 修 9 | 取代 D29 修 8 的 as_of 截止與 done。(1) Target 補建改為常駐工作（Target 端的對帳 ②，與 Source ② 對稱）：每個自查週期對每個 Source 沿 keyset 游標 (completed_at, identity) 續讀 `/pending?state=COMPLETED`，讀到空頁為止；只補缺列，既有列不碰；只補 source_ready_at 在 Target 保留規則內的 identity，避免與 D35 修 purge 互相拉扯。游標綁 Source incarnation，換代從頭讀；再次還原全部重設；本頁 upsert 與游標同交易、ENOENT 建 invalid + LOST、其他 stat 失敗本頁重試，同 D29 修 8 (2)。`rebuild_progress{source_node, incarnation, cursor, caught_up_at}`，caught_up_at = 本週期讀到空頁的時刻；`/locate` 的 UNAVAILABLE 條件改為「該 Source 自還原後尚未 caught up」。封閉反例：還原前在途的 DONE 在截止點後把 A 改成 COMPLETED，下一週期續讀補到 A，最壞晚一個週期，期間 `/locate` 回 DATA_NOT_READY 屬保守而非誤報。不引入 Target incarnation 圍籬：擋舊 DONE 需 hello 握手與同交易 last-seen 檢查，常駐續讀用既有機制即最終一致。(2) 列舉水位差：多執行緒 handler 的交易可能以較小的時間戳晚提交，Node 間所有 keyset 列舉只回傳時間戳 ≤ 伺服端 now − δ 的列，δ 為交易最長時間上限（operational policy，起始 60 s）；`/received?since=` 同。(3) `/locate` 判定順序限定 Target 角色（source ≠ 本 Node）才 Policy 先；source == 本 Node 依 file_identity 判定，Source 不在自己的 Required targets 裡不影響本地 Ready | RC-02, RC-04, RR-03, SR-06, D29 修 8, D19, D15 修, T19 |
| D29 修 10 | 取代 D29 修 9 (2) 的水位差 δ：延後讀取只是緩衝，時間戳較小的交易仍可能晚提交而永久落在游標後方。改用可證明已提交的前綴：Node 間兩個對帳列舉的鍵改為提交序號，`obligation.completed_seq`（供 Target ② 讀 COMPLETED）與 `received.published_seq`（供 Source ② 讀 `/received`）；序號在該交易的最後一步從單列計數器（`node_meta.completed_seq_last` / `published_seq_last`）以 `SELECT … FOR UPDATE` 取號並遞增，行鎖持有到 commit，後一交易取不到號直到前一交易提交，故提交順序 = 序號順序，序號 N 可見即所有 < N 已提交。keyset 鍵只剩一個整數，游標 = 最後看到的序號，不需 δ、不需 identity 作次鍵；completed_at / published_at 只留給指標與 age（D26）。代價：兩類提交各自序列化在一個行鎖上，鎖只在交易尾端持有；DONE 穩態 2.3 筆/s、追平時千筆/s，列入壓測。時效說法修正：健康情況下缺列補回 = 一個週期 + 本輪處理時間；遇 unknown 重試不保證上限。日常成本：每 Target 每日約讀 2.2×10⁶ 筆完成紀錄（9 Source × 10⁴/h × 24），已有列在 DB 先過濾，只有缺列才 stat NAS，列入負載驗收 | RC-02, RC-04, RR-03, D29 修 9, D26, T19 |
| D29 修 11 | D29 修 10 的取號契約寫精確。(1) 序號每列唯一：交易尾端一次保留 K 個連續號碼（K = 本交易變更的列數），逐列分配；計數器、資料、游標同交易提交；一頁補 201 列共用一號而列舉只回 200 列的漏列不會發生。(2) 換號範圍：`obligation.completed_seq` 在每次進入 COMPLETED 時換新號（DONE、對帳 ② 補帳、rebuild 匯入、重開後再次完成），重開時保留舊號不影響；`received.published_seq` 改名 `change_seq`，任何對外可見的列變更皆換新號（首次發布、標 invalid、換新事件、修復回 valid、換錨、恢復檢查回寫、recovery 旗標變化），否則後續變更落在游標後方，D55 修 3 讀不到。(3) 計數器分兩列：`seq_counter{name ∈ completed, change, last}`，Source 角色與 Target 角色各鎖各的，不共用 node_meta 同一列的行鎖。(4) 負載基準統一用 §7 的 10⁵ 檔/日/Source：Source DONE ≈ 9 × 10⁵ / 日 ≈ 10 筆/s，同 Node Target 發布 ≈ 10 筆/s，Target ② 每日每 Target 讀 ≈ 9 × 10⁵ 筆；若日量達 2.4 × 10⁵（10⁴/h）則各 25 筆/s、2.2 × 10⁶ 筆；追平時 DONE 峰值受每 Target 4 個全域槽（Source 面對 9 個 Target 合計最多 36 個 worker）與 `/pending` 40 筆/s 上限約束；「2.3 筆/s」是單一 (source, target) 追平時的檔案速率，不是 Source 總量 | RC-02, RR-03, D29 修 10, D31, D32, T19 |
| 事實 | 每 Node 平均每小時約 10⁴ 檔、一筆一檔，日量 2.4×10⁵，高於 spec §18 的 ≥10⁵ 下限；以此為計算起點，10⁵/日只作比較情境，取代 D29 修 11 (4) 的基準。以 9 個 Required targets 計：每 Source 穩態 DONE ≈ 25 筆/s；每 Target 每日新增完成紀錄讀取 ≈ 2.16×10⁶ 筆；30 天義務量 ≈ 6.48×10⁷ 列/Source；60 天已完成義務容量 ≈ 1.296×10⁸ 列/Source，另加未完成保留量；D36 記憶體 key 集合 3 小時約 3 萬筆。位元組：spec §18 的 10²–10³ GB/日是單一 Target 接收 9 個 Source 的合計；每 Target 每日 9 × 2.4×10⁵ = 2.16×10⁶ 檔，D31 取的 1 TB/日隱含平均約 463 KB；實際大小範圍 KB～5 MB，平均 1 MB 即每 Target 2.16 TB/日，平時 25 MB/s，停 24 h 後 24 h 追平連同新流量需 50 MB/s，D31 的 2× 餘裕只在 Target 合計 1 TB/日的待驗證前提下成立；Deep 30 天 × 1 TB = 30 TB 在 20 MB/s 純讀取約 17.4 天，平均 1 MB 時 65 TB 約 37.5 天，超過 30 天保留期。大小分佈為 §18 TBD，列 §12 待決 | §18, D31, D32, D36, D52, D29 修 11 |
| D10 修 | ingest 原子性：file_identity 與依 Policy 展開的全部 Required target obligation 同一交易提交，commit 後才更新記憶體已知 key 集合；部分失敗整筆回滾，下輪重試。對帳加 ①″：DB 保留中的全部 file_identity（含窗外）× 現行 Policy 缺少的 obligation 列只補缺列（PENDING、epoch 1），不改既有狀態；不只查 NAS 與 identity 差集、不只窗內。Policy 變更走遷移程序（§4.4），其對既有 identity 的補建亦由 ①″ 執行 | RR-02, RC-02, D10, D36, D56, T06, T11 |
| D12 修 | Target 正常與修復流程的關卡順序寫死：串流驗證通過 → 暫存內容持久化成功（fsync；NFSv3 UNSTABLE 寫入須 COMMIT 成功，digest 正確不代表已穩定儲存）→ link 或 rename 成功 → received 交易提交 → 才送 DONE。任一關卡失敗或結果未知都不得越過：持久化失敗刪暫存回 FAILED；link / rename 結果未知走 EEXIST 驗證路徑；received 提交失敗不送 DONE，下輪交付以 EEXIST 驗證補列。實際 OS / NFS client / NAS 組合的 failover 行為列驗收項 | RR-03, IR-02, D3, D12, D43, D44, T08, T13 |
| D23 修 2 | library 不快取 READY 判定：每次新 `read` 都向本 Node sync service 重新 `/locate`，invalid 列不回 READY；本 Node 服務不可達回 UNAVAILABLE，仍不依賴 Remote Node（SR-06）。已開啟的 InputStream 不撤銷，只保證查詢時已知 invalid 不授權新讀取。取代 D23 修的「快取 READY 結果」 | SR-06, IR-02, D15 修, D23 修, D54, T28 |
| D33 修 3 | 取代 D33 修 2 的 counter 增量告警：事故事件與 `acknowledged = false` 同一交易持久化在 `obligation_history`；告警 = `unacked_incident_count{kind}`（DB 查詢的 gauge）> 0；只有 ops `ack <event_id>` 寫 ops_audit 後才清，scrape 或送出告警都不清。封閉兩個窗口：DB 已提交、記憶體未遞增；遞增後未被 scrape 就重啟。`incident_events_total` 留作統計 | §16, IR-03, D33 修 2, D25, T31 |
| D14 修 | 取代 shared token：每 Node 獨立憑證（每 Node token，由 sync 主機本機秘密檔提供，不進 git；輪替為 ops 程序），伺服端由憑證得出呼叫者 Node 名；`/pending`、`/file`、`/report`、`/received` 的 `target` 參數必須等於呼叫者身分，否則 403。D40 的「Policy 已強制」與 F31 只在身分可證明時成立。仍不做 mTLS，避免引入 CA 與憑證輪替 | DG-04, §13, §17, D14, D40 |
| D30 修 5 | 影響發現保證與時間預算的參數在 v1 固定為常數，移出 operational policy 可調項：宣告年齡上限 N、Abandoned TTL、清道夫排程間隔、全量對帳週期、搜尋時窗 30 天、遲到發布停跑容忍 19 天。變更須走明定的遷移程序：所有 Node 同版、無在途宣告或明定過渡期、LKG 回退亦視為變更；啟用檢查只驗證整個預算不等式成立，不宣稱新設定能安全承接舊工作 | DG-05, D17, D30, D45, D56, T15, T22 |
| D14 修 2 | 端點依角色綁身分，取代 D14 修「四個端點一律綁 target」：`/pending`、`/file`、`/report` 的呼叫者是 Target，target 由認證身分取得，請求另帶且不同 → 403；`/received` 的呼叫者是 Source，只回傳 identity.source_node = 呼叫者的列，Target 是受呼叫的本 Node。驗證端只持有各 Node token 的雜湊，不持有可冒用他 Node 的明文 | DG-04, §13, D14 修, D40 |
| D12 修 2 | D12 修的「結果未知」收窄，接回 D51 / D43：link 或 rename 逾時時操作所有權與 in-flight 排他保留，不得先刪暫存、釋放排他或啟動替代發布；舊操作真正結束後才依其結果與原交付代次查證並更新 received，結束前不送 DONE、不重拉；rename 目標本就有正式檔，檔案存在不代表本次覆蓋已完成，不得以 EEXIST 驗證取代等待。儲存類錯誤走 D43 閘門，不視為單檔驗證失敗。持久化措辭改為「暫存 fsync 成功；必要的穩定寫入、COMMIT 與 verifier 處理由 OS NFS client 完成並經 failover 驗收」，Framework 不自行發 NFS RPC | RR-03, IR-02, SR-05, D12 修, D43, D51, D51 修, T13, T25 |
| D30 修 6 | D30 修 5 補三處：固定常數不接受 config 覆寫，設定若保留欄位必須等於 v1 固定值否則啟用拒絕，只驗不等式會放行縮窗等不相容變更；時鐘裕度 1 天列入固定預算；遷移範圍含 App library（N 在 library 執行），「所有 Node 同版」指 sync service 與 library 皆同版；具體遷移程序未定義前，v1 不支援變更這組常數 | DG-05, D17, D30 修 5, D45, D56, T15, T22 |
| 驗收審查 5 | 階段結論：D1～D56 及修訂已封閉，本輪未新增設計決策。剩餘兩項為驗收而非設計：NAS failover 驗收（D12 修 2，實際 OS / NFS client / NAS 組合下 fsync 後的穩定寫入與逾時操作恢復）、實際檔案大小分佈下的壓測（吞吐、延遲、追平餘裕、Deep 週期、自查與常駐對帳成本、seq 鎖競爭、rebuild 時間）。文件中的估算（1 TB/日、23 MB/s、17.4 天、2× 餘裕）列為前提不作承諾；量測後才填 system-design §10 與 D31 / D52 的最終值。下一步為實作計畫，NFS 語意實測與 failover 驗收先行 | 過程註記 |
| P01 偏差 | 實作計畫 `docs/superpowers/plans/P01-core-finalize.md` 定的四項：① manifest 目錄加 `.manifest/` 前綴（`<source>/<ns>/.manifest/<bucket>/`），避免與 3 hex 字元的 Data class 撞名；② 內容目錄日期時區為 PathLayout 建構參數，預設 Node 本地時區，Fab 內所有 Node 同一設定；③ Logical key 保留字尾（`.writing`、`.tmp` 結尾、含 `.manifest`）於 beginWrite 拒絕，列入 SR-01 Application 義務；④ 串流 write 經有界執行器，64 KB buffer 聚合，壓測若槽位被大檔寫入占滿再拆專用 pool | D2, D48, D51, SR-01, file-inventory |
| P01 偏差 2 | 實作期新增四項：⑤ library 的 finalize 方法命名為 `finalizeWrite()`（`finalize()` 與 `Object.finalize()` 衝突不可編譯），文件中的「Finalize」仍指協定；⑥ `NfsException` 為 Busy / Timeout 的共同抽象基底（含 `op()`），`WriteHandle.close()` 亦經有界執行器（關檔是 NFS 操作，卡住不得占用呼叫執行緒）；⑦ 宣告年齡上限 N 的判定依 D53 修用 manifest **mtime**，且只對「link manifest 遇 EEXIST 的既有宣告」檢查，新宣告不檢查；與 D56 ②（mtime 只作粗篩、不推斷義務狀態）有張力——manifest 本體已含 Source 打的 `source_ready_at`（D26），改用它可省一次 stat 並免除 NAS 時鐘誤差，**待使用者裁定**，未裁定前維持 mtime；⑧ Finalize 第③步 `link(<key>)` 遇 EEXIST 後重算 `<key>` digest 與宣告不符 → 回 `CONFLICT`（Application 換 key），已發布的 `<key>` 依 D46 不由 library 刪，留給 F21 / Deep check 處理，此終態 §6 / D53 未命名，補記於此 | D26, D46, D51, D53 修, D56, F21 |
